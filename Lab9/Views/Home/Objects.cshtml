@{
    ViewBag.Title = "Objects";
}
<h2>@ViewBag.Title.</h2>

@model List<Lab9.Models.objects>
<div class="container">
    <div class="row">
        <table class="table">
            <tr>
                <th>ID</th>
                <th>Name</th>
            </tr>

            @foreach (var obj in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(_ => obj.id)
                    </td>
                    <td>
                        @Html.DisplayFor(_ => obj.name)
                    </td>
                </tr>
            }
        </table>
    </div>
</div>
